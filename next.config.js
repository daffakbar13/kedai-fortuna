/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // assetPrefix: 'https://daffakbar13.github.io/kedai-fortuna',
  // webpack5: true,
  // webpack: (config) => {
  //   config.resolve.fallback = { fs: false }

  //   return config
  // },
}

module.exports = nextConfig
